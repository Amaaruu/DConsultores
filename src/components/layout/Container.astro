---
const { size = 'content', class: className = '' } = Astro.props;
const sizes = {
  reading: 'max-w-reading',
  content: 'max-w-content',
  wide: 'max-w-wide',
};
---

<div class:list={['mx-auto w-full px-5 sm:px-6 lg:px-8', sizes[size as keyof typeof sizes], className]}>
  <slot />
</div>
