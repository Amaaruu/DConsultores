---
const { src, alt, width = 1200, height = 675, loading = 'lazy', class: className = '' } = Astro.props;
---

<img src={src} alt={alt} width={width} height={height} loading={loading} decoding="async" class:list={['h-auto w-full rounded-card border border-border object-cover', className]} />
